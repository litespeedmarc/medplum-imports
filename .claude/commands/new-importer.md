Generate a new Medplum importer from GitHub issue $ARGUMENTS.

Invoke the `ivo-the-import-generator` agent with issue number $ARGUMENTS.
The agent will work through all 5 phases, maintaining a session log throughout.

If no issue number is provided, ask the user: "Which GitHub issue should I work from?"
